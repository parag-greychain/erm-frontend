import type React from "react";
import { ChatPanel } from "../../components";

const Chat: React.FC = () => {
  return (
    <div className="chat-page">
      <ChatPanel />
    </div>
  );
};

export default Chat;
